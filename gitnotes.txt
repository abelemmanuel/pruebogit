Notas sobre curso de git
https://www.youtube.com/watch?v=HiXLkL42tMU


Estados basicos 
- working directory
- staging area
- repository

basico
git init
git add pasar del working directory a staging
git status
git commit del staging al repository
git push para subirlo a un repository remoto
git pull para traer los cambios 
git clone hace una copia del server central a la pc


Inicializar repo de codigo.

#Comienza el proyecto. Administra una carpeta en git desde aca. No hay que tocarlo
git init

#Para seguimiento de lo que estoy haciendo
git status

# para ir agregando archivos al staging area
git add nombrearchivo

git config --global user.email "email"
git config --global user.name "name"

git commit
se escribe un mensaje....

git log 
para ver los commits que hemos hecho


git checkout -- nombrearchivo
para descartar los cambios de un archivos

git diff nombrearchivo


.gitignore
carpeta que quiero ignorar

git commit -m "para no abrir el editor y cargar el mensaje"

git branch

git branch nombrebranch

git checkout login
entro a la nueva rama


git add . 
agrego todo


